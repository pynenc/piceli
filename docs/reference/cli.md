<!-- Generated by `python -m piceli.reference_docs` from `piceli help-json`. Do not edit by hand. -->

# Command reference

Every `piceli` command with its options and its contract: what it reads and writes, whether it contacts a cluster, whether it needs approval and whether it is safe to retry. The same data is available as JSON from `piceli help-json` (or `piceli --help-json`).

## Conventions

- **stdout** is for machine output: one JSON object (JSON lines for streaming commands).
- **stderr** is for human text: summaries and hints.
- A refusal prints `{"state": "rejected", "reason": "<code>"}` and exits `2`; see {doc}`errors` or run `piceli explain <code>`.
- **Output contract** `conforms` means the command follows these rules exactly; `partial` means it prints JSON but its refusals do not yet use the rejection shape above.

| Exit code | Meaning |
| --- | --- |
| `0` | success |
| `1` | the operation ran but did not succeed (not ready, drift, build failed) |
| `2` | rejected before any change (stdout: the rejection object) |
| `3` | approval required; nothing was executed |

## Commands

| Command | Summary | Cluster | Approval |
| --- | --- | --- | --- |
| [`piceli artifacts build`](#cli-artifacts-build) | Assemble an OCI image layout from a plan without running code. | none | no |
| [`piceli artifacts build-spec preview`](#cli-artifacts-build-spec-preview) | Preview a containerized build and its plan hash. | none | no |
| [`piceli artifacts build-spec run`](#cli-artifacts-build-spec-run) | Run an approved containerized build and write a receipt. | none | yes |
| [`piceli artifacts deliver`](#cli-artifacts-deliver) | Deliver a digest-approved image to a registry or node. | reads | yes |
| [`piceli artifacts execute-command`](#cli-artifacts-execute-command) | Run a pinned external build command under an approved plan. | none | yes |
| [`piceli artifacts import-local`](#cli-artifacts-import-local) | Import an OCI layout into the local Docker daemon. | none | yes |
| [`piceli artifacts inspect`](#cli-artifacts-inspect) | Recheck an OCI layout and print its summary. | none | no |
| [`piceli artifacts pin`](#cli-artifacts-pin) | Pin one public source file by digest. | none | no |
| [`piceli artifacts preview`](#cli-artifacts-preview) | Preview a deterministic OCI build plan (no tools run). | none | no |
| [`piceli artifacts preview-command`](#cli-artifacts-preview-command) | Preview a pinned external build command. | none | no |
| [`piceli explain`](#cli-explain) | Explain an error code: cause, fix and whether a retry can succeed. | none | no |
| [`piceli help-json`](#cli-help-json) | Print the whole CLI tree (commands, options, contracts) as JSON. | none | no |
| [`piceli inputs record`](#cli-inputs-record) | Capture each declared source (or the ``--only`` ones) and write a lock. | none | no |
| [`piceli inputs verify`](#cli-inputs-verify) | Recapture the sources and compare them with the lock (exit 1 on drift). | none | no |
| [`piceli observe forward-command`](#cli-observe-forward-command) | Print a JSON argv array for one explicit loopback-only port forward. | none | no |
| [`piceli observe forward-list`](#cli-observe-forward-list) | List a user's saved port-forward preferences without starting a process. | none | no |
| [`piceli observe forward-run`](#cli-observe-forward-run) | Run one saved loopback-only port forward until the caller interrupts it. | reads | no |
| [`piceli observe forward-save`](#cli-observe-forward-save) | Persist one harmless port-forward preference for a local user. | none | no |
| [`piceli observe forwards apply`](#cli-observe-forwards-apply) | Start every declared forward and supervise it in the foreground. | reads | no |
| [`piceli observe forwards status`](#cli-observe-forwards-status) | Probe each declared forward's loopback endpoint once and print JSON. | none | no |
| [`piceli observe logs-command`](#cli-observe-logs-command) | Print JSON argv for a bounded, explicit workload-log request. | none | no |
| [`piceli observe logs-run`](#cli-observe-logs-run) | Run a bounded workload-log request in the caller's foreground terminal. | reads | no |
| [`piceli observe serve`](#cli-observe-serve) | Open the local operations dashboard and optionally restore saved forwards. | reads | no |
| [`piceli observe status`](#cli-observe-status) | Print declared resources, live state, and objects absent from the archive. | reads | no |
| [`piceli operator approve`](#cli-operator-approve) | Record an explicit operator approval for a pull request rollout. | none | yes |
| [`piceli operator backup`](#cli-operator-backup) | Create a verified, mode-restricted backup archive of operator state. | none | no |
| [`piceli operator promote`](#cli-operator-promote) | Promote an existing built digest to a new tag without rebuilding. | none | no |
| [`piceli operator restore`](#cli-operator-restore) | Safely verify and restore operator state into empty destination. | none | no |
| [`piceli operator serve`](#cli-operator-serve) | Launch the Piceli Operator dashboard and unified REST API. | reads | no |
| [`piceli operator status`](#cli-operator-status) | Print classified operator inventory: managed, unmanaged, unknown, and releases. | reads | no |
| [`piceli release apply`](#cli-release-apply) | Execute an approved plan (``--approve HASH``), or plan and confirm. | writes | yes |
| [`piceli release plan`](#cli-release-plan) | Capture live discovery and persist an approvable plan (prints its hash). | reads | no |
| [`piceli release preview`](#cli-release-preview) | Alias of `plan`. | reads | no |
| [`piceli release resume`](#cli-release-resume) | Resume an interrupted apply of a created release (same grant and ids). | writes | no |
| [`piceli release rollback`](#cli-release-rollback) | Re-plan and re-apply an earlier release against current cluster state. | writes | yes |
| [`piceli release secret show`](#cli-release-secret-show) | Show a secret's metadata, and its value with --reveal (never logged). | none | no |
| [`piceli release status`](#cli-release-status) | Show catalogued releases, their executions and history (no cluster access). | none | no |
| [`piceli release stop`](#cli-release-stop) | Cancel the latest execution of a release (exact owner only). | reads | no |
| [`piceli render`](#cli-render) | Print the manifests of a typed app or composition. Never contacts a cluster. | none | no |

(cli-artifacts-build)=
### `piceli artifacts build`

Assemble an OCI image layout from a plan without running code.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--plan` | path | required |  |
| `--source-root` | path | required |  |
| `--output` | path | required |  |

**Contract**

- **Reads:** plan JSON, source root
- **Writes:** --output layout
- **Cluster:** none
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-artifacts-build-spec-preview)=
### `piceli artifacts build-spec preview`

Preview a containerized build and its plan hash.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--spec` | path | required |  |
| `--inputs` | path |  |  |

**Contract**

- **Reads:** build.toml, inputs.toml, build contexts
- **Writes:** nothing (read-only)
- **Cluster:** none
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-artifacts-build-spec-run)=
### `piceli artifacts build-spec run`

Run an approved containerized build and write a receipt.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--spec` | path | required |  |
| `--inputs` | path |  |  |
| `--lock` | path |  |  |
| `--approve-builder` | text | required |  |
| `--approve-plan` | text |  |  |
| `--allow-network` | boolean | `False` |  |
| `--out` | path | required |  |
| `--output-dir` | path |  |  |
| `--docker` | path |  |  |
| `--docker-sha256` | text |  |  |
| `--log` | path |  |  |
| `--progress` | text | `steps` | stderr during the build: one line per step (default), plus the raw build output (plain), or nothing (quiet) |
| `--max-seconds` | float | `7200.0` |  |

**Contract**

- **Reads:** build.toml, inputs, build contexts, docker
- **Writes:** --out receipt, --output-dir, local Docker image store
- **Cluster:** none
- **Approval required:** yes
- **Safe to retry:** yes
- **Exit codes:** `0` success, `1` the operation ran but did not succeed (not ready, drift, build failed), `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-artifacts-deliver)=
### `piceli artifacts deliver`

Deliver a digest-approved image to a registry or node.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--image` | text |  |  |
| `--archive` | path |  |  |
| `--to` | text | required |  |
| `--approve-digest` | text | required |  |
| `--ref` | text |  |  |
| `--docker` | path |  |  |
| `--docker-sha256` | text |  |  |
| `--docker-socket` | path |  |  |
| `--ssh` | path |  |  |
| `--ssh-sha256` | text |  |  |
| `--ssh-agent-socket` | path |  |  |
| `--timeout` | float | `600` |  |
| `--receipt` | path |  |  |
| `--journal` | path |  |  |
| `--node-registry` | text |  |  |
| `--credentials` | path |  |  |
| `--ca-file` | path |  |  |
| `--via-forward` | text |  |  |
| `--namespace` | text |  |  |
| `--kubeconfig` | path |  |  |
| `--context` | text |  |  |
| `--forward-remote-port` | integer | `5000` |  |
| `--kubectl` | path |  |  |
| `--kubectl-sha256` | text |  |  |

Mutually exclusive: `archive` / `image`.

**Contract**

- **Reads:** docker image or archive, credentials file, kubeconfig
- **Writes:** registry or node image store, --receipt, --journal
- **Cluster:** reads
- **Approval required:** yes
- **Safe to retry:** yes
- **Exit codes:** `0` success, `1` the operation ran but did not succeed (not ready, drift, build failed), `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial
- **Notes:** Registry pushes are content-addressed and idempotent. Contacts the cluster only for --via-forward (port-forward).

(cli-artifacts-execute-command)=
### `piceli artifacts execute-command`

Run a pinned external build command under an approved plan.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--spec` | path | required |  |
| `--source-root` | path | required |  |
| `--approve-plan` | text | required |  |
| `--allow-code-execution` | boolean | `False` |  |
| `--allow-network` | boolean | `False` |  |
| `--timeout` | float | `60` |  |

**Contract**

- **Reads:** command spec, source root, pinned tool
- **Writes:** tool outputs
- **Cluster:** none
- **Approval required:** yes
- **Safe to retry:** no
- **Exit codes:** `0` success, `1` the operation ran but did not succeed (not ready, drift, build failed), `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-artifacts-import-local)=
### `piceli artifacts import-local`

Import an OCI layout into the local Docker daemon.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--layout` | path | required |  |
| `--docker` | path | required |  |
| `--docker-sha256` | text | required |  |
| `--socket` | path | required |  |
| `--approve-digest` | text | required |  |

**Contract**

- **Reads:** OCI layout, docker
- **Writes:** local Docker image store
- **Cluster:** none
- **Approval required:** yes
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-artifacts-inspect)=
### `piceli artifacts inspect`

Recheck an OCI layout and print its summary.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--layout` | path | required |  |

**Contract**

- **Reads:** OCI layout
- **Writes:** nothing (read-only)
- **Cluster:** none
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-artifacts-pin)=
### `piceli artifacts pin`

Pin one public source file by digest.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--source-root` | path | required |  |
| `--public-file` | text | required |  |

**Contract**

- **Reads:** source file
- **Writes:** nothing (read-only)
- **Cluster:** none
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-artifacts-preview)=
### `piceli artifacts preview`

Preview a deterministic OCI build plan (no tools run).

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--plan` | path | required |  |

**Contract**

- **Reads:** plan JSON
- **Writes:** nothing (read-only)
- **Cluster:** none
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-artifacts-preview-command)=
### `piceli artifacts preview-command`

Preview a pinned external build command.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--spec` | path | required |  |

**Contract**

- **Reads:** command spec
- **Writes:** nothing (read-only)
- **Cluster:** none
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-explain)=
### `piceli explain`

Explain an error code: cause, fix and whether a retry can succeed.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `CODE` | text | required |  |
| `--json` | boolean | `False` | Print the entry as one JSON object |

**Contract**

- **Reads:** nothing
- **Writes:** nothing (read-only)
- **Cluster:** none
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** conforms

(cli-help-json)=
### `piceli help-json`

Print the whole CLI tree (commands, options, contracts) as JSON.

No options.

**Contract**

- **Reads:** nothing
- **Writes:** nothing (read-only)
- **Cluster:** none
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success
- **Output contract:** conforms

(cli-inputs-record)=
### `piceli inputs record`

Capture each declared source (or the ``--only`` ones) and write a lock.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--spec` | path | required | inputs.toml declaring the sources |
| `--out` | path |  | Write the lock here (default: stdout only) |
| `--timeout` | float | `30.0` | Seconds allowed for each git call |
| `--only` | text (repeatable) |  | Limit to this declared source (repeatable); same spec as the build |

**Contract**

- **Reads:** inputs.toml, git work trees
- **Writes:** --out lock file
- **Cluster:** none
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial
- **Notes:** Refusal reasons are free text today (D0b).

(cli-inputs-verify)=
### `piceli inputs verify`

Recapture the sources and compare them with the lock (exit 1 on drift).

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--spec` | path | required | inputs.toml declaring the sources |
| `--lock` | path | required | Lock written by `inputs record` |
| `--timeout` | float | `30.0` | Seconds allowed for each git call |
| `--only` | text (repeatable) |  | Limit to this declared source (repeatable); same spec as the build |

**Contract**

- **Reads:** inputs.toml, lock, git work trees
- **Writes:** nothing (read-only)
- **Cluster:** none
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `1` the operation ran but did not succeed (not ready, drift, build failed), `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial
- **Notes:** Refusal reasons are free text today (D0b).

(cli-observe-forward-command)=
### `piceli observe forward-command`

Print a JSON argv array for one explicit loopback-only port forward.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--user` | text | required |  |
| `--name` | text | required |  |
| `--kubeconfig` | path | required |  |
| `--context` | text |  |  |
| `--kubectl` | text | `kubectl` |  |
| `--preferences` | path |  |  |

**Contract**

- **Reads:** preferences file
- **Writes:** nothing (read-only)
- **Cluster:** none
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-observe-forward-list)=
### `piceli observe forward-list`

List a user's saved port-forward preferences without starting a process.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--user` | text | required |  |
| `--preferences` | path |  |  |

**Contract**

- **Reads:** preferences file
- **Writes:** nothing (read-only)
- **Cluster:** none
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-observe-forward-run)=
### `piceli observe forward-run`

Run one saved loopback-only port forward until the caller interrupts it.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--user` | text | required |  |
| `--name` | text | required |  |
| `--kubeconfig` | path | required |  |
| `--context` | text |  |  |
| `--kubectl` | text | `kubectl` |  |
| `--preferences` | path |  |  |

**Contract**

- **Reads:** preferences file, kubeconfig
- **Writes:** nothing (read-only)
- **Cluster:** reads
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-observe-forward-save)=
### `piceli observe forward-save`

Persist one harmless port-forward preference for a local user.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--user` | text | required |  |
| `--name` | text | required |  |
| `--namespace` | text | required |  |
| `--target` | text | required | service/NAME, deployment/NAME or pod/NAME |
| `--local-port` | integer | required |  |
| `--remote-port` | integer | required |  |
| `--preferences` | path |  |  |

**Contract**

- **Reads:** session archive
- **Writes:** preferences file
- **Cluster:** none
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-observe-forwards-apply)=
### `piceli observe forwards apply`

Start every declared forward and supervise it in the foreground.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--profile` | path | required | Access profile: the --ui-config TOML format ([[shortcuts]] with optional [shortcuts.health] and [shortcuts.restart]) |
| `--kubeconfig` | path | required |  |
| `--context` | text |  |  |
| `--namespace` | text |  | Namespace for shortcuts that pin none |
| `--only` | text (repeatable) |  | Supervise only these shortcut ids |
| `--kubectl` | text | `kubectl` |  |
| `--poll` | float | `1.0` | Status report cadence (seconds) |

**Contract**

- **Reads:** access profile, kubeconfig
- **Writes:** nothing (read-only)
- **Cluster:** reads
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-observe-forwards-status)=
### `piceli observe forwards status`

Probe each declared forward's loopback endpoint once and print JSON.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--profile` | path | required |  |
| `--only` | text (repeatable) |  | Check only these shortcut ids |

**Contract**

- **Reads:** access profile
- **Writes:** nothing (read-only)
- **Cluster:** none
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `1` the operation ran but did not succeed (not ready, drift, build failed)
- **Output contract:** partial

(cli-observe-logs-command)=
### `piceli observe logs-command`

Print JSON argv for a bounded, explicit workload-log request.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--namespace` | text | required |  |
| `--target` | text | required | pod/NAME, deployment/NAME, or another workload |
| `--kubeconfig` | path | required |  |
| `--context` | text |  |  |
| `--tail` | integer | `200` |  |
| `--container` | text |  |  |
| `--previous`, `--no-previous` | boolean | `False` |  |
| `--kubectl` | text | `kubectl` |  |

**Contract**

- **Reads:** nothing
- **Writes:** nothing (read-only)
- **Cluster:** none
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-observe-logs-run)=
### `piceli observe logs-run`

Run a bounded workload-log request in the caller's foreground terminal.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--namespace` | text | required |  |
| `--target` | text | required | pod/NAME, deployment/NAME, or another workload |
| `--kubeconfig` | path | required |  |
| `--context` | text |  |  |
| `--tail` | integer | `200` |  |
| `--container` | text |  |  |
| `--previous`, `--no-previous` | boolean | `False` |  |
| `--kubectl` | text | `kubectl` |  |

**Contract**

- **Reads:** kubeconfig
- **Writes:** nothing (read-only)
- **Cluster:** reads
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-observe-serve)=
### `piceli observe serve`

Open the local operations dashboard and optionally restore saved forwards.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--archive` | path | required |  |
| `--kubeconfig` | path | required |  |
| `--context` | text |  |  |
| `--namespace` | text |  | Namespace for shortcuts and pods (default: the archive's) |
| `--preferences` | path |  |  |
| `--user` | text |  | Restore this user's saved forwards |
| `--port` | integer | `9876` |  |
| `--ui-config` | path | env `PICELI__UI_CONFIG` | TOML file with dashboard shortcuts, topology tiers, and badges (also $PICELI__UI_CONFIG) |
| `--start-shortcuts`, `--no-start-shortcuts` | boolean | `False` | Start and health-supervise every configured shortcut (port-conflict preflight first) |

**Contract**

- **Reads:** session archive, kubeconfig, preferences file
- **Writes:** preferences file
- **Cluster:** reads
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-observe-status)=
### `piceli observe status`

Print declared resources, live state, and objects absent from the archive.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--archive` | path | required |  |
| `--kubeconfig` | path | required |  |
| `--context` | text |  |  |
| `--include-common-types`, `--no-include-common-types` | boolean | `True` |  |

**Contract**

- **Reads:** session archive, kubeconfig
- **Writes:** nothing (read-only)
- **Cluster:** reads
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-operator-approve)=
### `piceli operator approve`

Record an explicit operator approval for a pull request rollout.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--state-dir` | path | required |  |
| `--pr-id` | integer | required |  |
| `--commit` | text | required |  |
| `--namespace` | text | required |  |
| `--approved-by` | text | required |  |

**Contract**

- **Reads:** nothing
- **Writes:** state_dir
- **Cluster:** none
- **Approval required:** yes
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-operator-backup)=
### `piceli operator backup`

Create a verified, mode-restricted backup archive of operator state.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--state-dir` | path | required |  |
| `--output` | path | required | Target .tar.gz archive path |

**Contract**

- **Reads:** state_dir
- **Writes:** backup archive
- **Cluster:** none
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-operator-promote)=
### `piceli operator promote`

Promote an existing built digest to a new tag without rebuilding.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--catalog` | path | required |  |
| `--source` | text | required | Source release name to promote |
| `--target` | text | required | Target tag/release name |

**Contract**

- **Reads:** catalog
- **Writes:** catalog
- **Cluster:** none
- **Approval required:** no
- **Safe to retry:** no
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-operator-restore)=
### `piceli operator restore`

Safely verify and restore operator state into empty destination.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--archive-file` | path | required |  |
| `--destination` | path | required | Target directory to restore into |

**Contract**

- **Reads:** backup archive
- **Writes:** destination directory
- **Cluster:** none
- **Approval required:** no
- **Safe to retry:** no
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-operator-serve)=
### `piceli operator serve`

Launch the Piceli Operator dashboard and unified REST API.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--kubeconfig` | path | required |  |
| `--namespace` | text | `default` |  |
| `--context` | text |  |  |
| `--archive` | path |  |  |
| `--catalog` | path |  |  |
| `--preferences` | path |  |  |
| `--state-dir` | path |  |  |
| `--user` | text |  |  |
| `--port` | integer | `9876` |  |
| `--ui-config` | path | env `PICELI__UI_CONFIG` | TOML file with dashboard shortcuts, topology tiers, and badges (also $PICELI__UI_CONFIG) |

**Contract**

- **Reads:** kubeconfig, catalog, archive, preferences file
- **Writes:** preferences file, state_dir
- **Cluster:** reads
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-operator-status)=
### `piceli operator status`

Print classified operator inventory: managed, unmanaged, unknown, and releases.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--kubeconfig` | path | required |  |
| `--namespace` | text | `default` |  |
| `--context` | text |  |  |
| `--archive` | path |  |  |
| `--catalog` | path |  |  |
| `--include-common-types`, `--no-include-common-types` | boolean | `True` |  |

**Contract**

- **Reads:** kubeconfig, archive, catalog
- **Writes:** nothing (read-only)
- **Cluster:** reads
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-release-apply)=
### `piceli release apply`

Execute an approved plan (``--approve HASH``), or plan and confirm.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--spec` | path | required | release.toml describing the release |
| `--approve` | text |  | Plan hash to execute (from a previous `plan`/`rollback` output) |
| `--auto-approve` | boolean | `False` | Plan and execute without confirmation (CI) |
| `--rotate` | text (repeatable) |  | Regenerate this secret generator's values in the new release (repeatable) |
| `--adopt` | text (repeatable) |  | Authorize adopting this existing object, as Kind/name or apiVersion/Kind/name (repeatable; adds to [release] adopt) |
| `--replace` | text (repeatable) |  | Authorize deleting this existing unmanaged object and creating it from the release, after writing a restorable backup (repeatable; adds to [release] replace; never retained or managed objects) |
| `--adopt-all-desired` | boolean | `False` | Authorize adopting every existing unmanaged object the composition declares (each is listed in the plan and bound to its hash) |

**Contract**

- **Reads:** release.toml, composition, state_dir, kubeconfig
- **Writes:** state_dir (catalog, journal, secret store)
- **Cluster:** writes
- **Approval required:** yes
- **Safe to retry:** no
- **Exit codes:** `0` success, `1` the operation ran but did not succeed (not ready, drift, build failed), `2` rejected before any change (stdout: the rejection object), `3` approval required; nothing was executed
- **Output contract:** partial
- **Notes:** After an interruption use `release resume`, not apply.

(cli-release-plan)=
### `piceli release plan`

Capture live discovery and persist an approvable plan (prints its hash).

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--spec` | path | required | release.toml describing the release |
| `--rotate` | text (repeatable) |  | Regenerate this secret generator's values in the new release (repeatable) |
| `--adopt` | text (repeatable) |  | Authorize adopting this existing object, as Kind/name or apiVersion/Kind/name (repeatable; adds to [release] adopt) |
| `--replace` | text (repeatable) |  | Authorize deleting this existing unmanaged object and creating it from the release, after writing a restorable backup (repeatable; adds to [release] replace; never retained or managed objects) |
| `--adopt-all-desired` | boolean | `False` | Authorize adopting every existing unmanaged object the composition declares (each is listed in the plan and bound to its hash) |
| `--out` | path |  | Also write the full redacted plan JSON here |

**Contract**

- **Reads:** release.toml, composition, state_dir, kubeconfig
- **Writes:** state_dir (pending plan, secret candidates), --out file
- **Cluster:** reads
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial
- **Notes:** Never changes the cluster. Prints the plan hash to approve.

(cli-release-preview)=
### `piceli release preview`

Alias of `plan`.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--spec` | path | required | release.toml describing the release |
| `--rotate` | text (repeatable) |  | Regenerate this secret generator's values in the new release (repeatable) |
| `--adopt` | text (repeatable) |  | Authorize adopting this existing object, as Kind/name or apiVersion/Kind/name (repeatable; adds to [release] adopt) |
| `--replace` | text (repeatable) |  | Authorize deleting this existing unmanaged object and creating it from the release, after writing a restorable backup (repeatable; adds to [release] replace; never retained or managed objects) |
| `--adopt-all-desired` | boolean | `False` | Authorize adopting every existing unmanaged object the composition declares (each is listed in the plan and bound to its hash) |
| `--out` | path |  | Also write the full redacted plan JSON here |

**Contract**

- **Reads:** release.toml, composition, state_dir, kubeconfig
- **Writes:** state_dir (pending plan, secret candidates), --out file
- **Cluster:** reads
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-release-resume)=
### `piceli release resume`

Resume an interrupted apply of a created release (same grant and ids).

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--spec` | path | required | release.toml describing the release |
| `--release` | text |  | Release name (default: the latest execution) |

**Contract**

- **Reads:** release.toml, composition, state_dir, kubeconfig
- **Writes:** state_dir (journal)
- **Cluster:** writes
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `1` the operation ran but did not succeed (not ready, drift, build failed), `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial
- **Notes:** Continues an already approved execution; needs no new approval.

(cli-release-rollback)=
### `piceli release rollback`

Re-plan and re-apply an earlier release against current cluster state.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `TARGET` | text | required |  |
| `--spec` | path | required | release.toml describing the release |
| `--approve` | text |  | Plan hash to execute (from a previous `plan`/`rollback` output) |
| `--auto-approve` | boolean | `False` | Plan and execute without confirmation (CI) |
| `--adopt` | text (repeatable) |  | Authorize adopting this existing object, as Kind/name or apiVersion/Kind/name (repeatable; adds to [release] adopt) |
| `--replace` | text (repeatable) |  | Authorize deleting this existing unmanaged object and creating it from the release, after writing a restorable backup (repeatable; adds to [release] replace; never retained or managed objects) |
| `--adopt-all-desired` | boolean | `False` | Authorize adopting every existing unmanaged object the composition declares (each is listed in the plan and bound to its hash) |

**Contract**

- **Reads:** release.toml, composition, state_dir, kubeconfig
- **Writes:** state_dir (catalog, journal)
- **Cluster:** writes
- **Approval required:** yes
- **Safe to retry:** no
- **Exit codes:** `0` success, `1` the operation ran but did not succeed (not ready, drift, build failed), `2` rejected before any change (stdout: the rejection object), `3` approval required; nothing was executed
- **Output contract:** partial

(cli-release-secret-show)=
### `piceli release secret show`

Show a secret's metadata, and its value with --reveal (never logged).

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `NAME` | text | required |  |
| `--spec` | path | required | release.toml describing the release |
| `--key` | text |  | One output, e.g. crt, key, ca.crt, cache.crt |
| `--release` | text |  | Release name (default: the latest execution) |
| `--reveal` | boolean | `False` | Print the value (otherwise asks on a terminal) |
| `--json` | boolean | `False` | Print JSON: metadata only unless --reveal |

**Contract**

- **Reads:** release.toml, state_dir (secret store)
- **Writes:** nothing (read-only)
- **Cluster:** none
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial
- **Notes:** Owner only. Never contacts the cluster; values print only with --reveal or a terminal confirmation.

(cli-release-status)=
### `piceli release status`

Show catalogued releases, their executions and history (no cluster access).

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--spec` | path | required | release.toml describing the release |

**Contract**

- **Reads:** release.toml, state_dir
- **Writes:** nothing (read-only)
- **Cluster:** none
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial

(cli-release-stop)=
### `piceli release stop`

Cancel the latest execution of a release (exact owner only).

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--spec` | path | required | release.toml describing the release |
| `--release` | text |  | Release name (default: the latest execution) |

**Contract**

- **Reads:** release.toml, composition, state_dir, kubeconfig
- **Writes:** state_dir (journal)
- **Cluster:** reads
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial
- **Notes:** Checks the cluster identity; records the cancellation locally.

(cli-render)=
### `piceli render`

Print the manifests of a typed app or composition. Never contacts a cluster.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `TARGET` | text |  |  |
| `--spec` | path |  | release.toml providing the namespace, images, secret inputs (as placeholders), [values] and declared nodes |
| `--namespace` | text |  | Namespace to render into (default: the spec's, else 'default') |
| `--format` | choice | `yaml` | Output format |

**Contract**

- **Reads:** module/app file, release.toml (optional), local receipts
- **Writes:** nothing (read-only)
- **Cluster:** none
- **Approval required:** no
- **Safe to retry:** yes
- **Exit codes:** `0` success, `2` rejected before any change (stdout: the rejection object)
- **Output contract:** partial
- **Notes:** Never contacts a cluster; secret values are placeholders.
