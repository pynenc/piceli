1. **Piceli**: typed Python apps (`App`), typed per-environment overrides, and a plan computed against the live cluster that you approve by hash before it is applied.
2. **cdk8s**: generates manifests from Python, but leaves applying them to another tool.
3. **Pulumi** (Python SDK): infrastructure as code with previews, with its own state backend.
